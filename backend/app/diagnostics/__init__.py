"""Deterministic failure diagnostic classifier. Maps raw gateway error codes
to the structured failure taxonomy defined in the domain specification.
Pure functions only — no database writes, no network calls."""
